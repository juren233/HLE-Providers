name: Build Provider Pack

on:
  workflow_dispatch:
    inputs:
      provider:
        description: Provider to publish
        required: true
        type: choice
        options:
          - netease
          - qqmusic
          - kugou
          - kuwo
          - qishui
          - salt-player
          - spotify
  push:
    tags:
      - "netease-v*"
      - "qqmusic-v*"
      - "kugou-v*"
      - "kuwo-v*"
      - "qishui-v*"
      - "salt-player-v*"
      - "spotify-v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"
      - uses: android-actions/setup-android@v4
      - name: Resolve provider from tag
        id: provider
        run: |
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            provider='${{ inputs.provider }}'
          else
            provider="${GITHUB_REF_NAME%%-v*}"
          fi
          case "$provider" in
            netease) display_name="网易云音乐" ;;
            qqmusic) display_name="QQ音乐" ;;
            kugou) display_name="酷狗音乐" ;;
            kuwo) display_name="酷我音乐" ;;
            qishui) display_name="汽水音乐" ;;
            salt-player) display_name="椒盐音乐" ;;
            spotify) display_name="Spotify" ;;
            *) echo "Unsupported provider tag: $GITHUB_REF_NAME" >&2; exit 1 ;;
          esac
          echo "id=$provider" >> "$GITHUB_OUTPUT"
          echo "display_name=$display_name" >> "$GITHUB_OUTPUT"
      - name: Restore signing key
        env:
          HLE_PROVIDER_SIGNING_KEY_PEM: ${{ secrets.HLE_PROVIDER_SIGNING_KEY_PEM }}
        run: |
          test -n "$HLE_PROVIDER_SIGNING_KEY_PEM"
          printf '%s' "$HLE_PROVIDER_SIGNING_KEY_PEM" > signing-private.pem
          chmod 600 signing-private.pem
      - name: Build signed Provider Pack
        id: pack
        run: |
          provider='${{ steps.provider.outputs.id }}'
          version_name=$(python3 -c 'import json, pathlib, sys; p=sys.argv[1]; print(json.loads(pathlib.Path(f"providers/{p}/provider.json").read_text())["versionName"])' "$provider")
          version_code=$(python3 -c 'import json, pathlib, sys; p=sys.argv[1]; print(json.loads(pathlib.Path(f"providers/{p}/provider.json").read_text())["versionCode"])' "$provider")
          if [ "$GITHUB_EVENT_NAME" = "workflow_dispatch" ]; then
            release_tag="${provider}-v${version_name}"
          else
            release_tag="$GITHUB_REF_NAME"
          fi
          apk="providers/$provider/build/outputs/apk/release/${provider}-release-unsigned.apk"
          asset="dist/$provider-$version_name.hlp"
          ./gradlew --no-daemon --max-workers=2 ":providers:$provider:assembleRelease"
          python3 scripts/build_provider_pack.py \
            --apk "$apk" \
            --manifest "providers/$provider/provider.json" \
            --private-key signing-private.pem \
            --output "$asset"
          pack_sha=$(sha256sum "$asset" | cut -d' ' -f1)
          echo "provider=$provider" >> "$GITHUB_OUTPUT"
          echo "version_name=$version_name" >> "$GITHUB_OUTPUT"
          echo "version_code=$version_code" >> "$GITHUB_OUTPUT"
          echo "release_tag=$release_tag" >> "$GITHUB_OUTPUT"
          echo "asset=$asset" >> "$GITHUB_OUTPUT"
          echo "sha256=$pack_sha" >> "$GITHUB_OUTPUT"
      - name: Publish Release asset
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          provider='${{ steps.pack.outputs.provider }}'
          display_name='${{ steps.provider.outputs.display_name }}'
          version_name='${{ steps.pack.outputs.version_name }}'
          release_tag='${{ steps.pack.outputs.release_tag }}'
          asset='${{ steps.pack.outputs.asset }}'
          if gh release view "$release_tag" >/dev/null 2>&1; then
            gh release upload "$release_tag" "$asset" --clobber
          else
            gh release create "$release_tag" "$asset" \
              --title "$display_name Provider $version_name" \
              --notes "HyperLyrics Enhanced 官方 $display_name Provider 插件包。"
          fi
          gh release edit "$release_tag" \
            --title "$display_name Provider $version_name" \
            --notes "HyperLyrics Enhanced 官方 $display_name Provider 插件包。"
      - name: Update signed provider catalog
        run: |
          provider='${{ steps.pack.outputs.provider }}'
          version_name='${{ steps.pack.outputs.version_name }}'
          version_code='${{ steps.pack.outputs.version_code }}'
          release_tag='${{ steps.pack.outputs.release_tag }}'
          pack_sha='${{ steps.pack.outputs.sha256 }}'
          asset_url="https://github.com/${GITHUB_REPOSITORY}/releases/download/${release_tag}/${provider}-${version_name}.hlp"

          git fetch origin main
          git switch -C main origin/main
          python3 - "$provider" "$version_name" "$version_code" "$asset_url" "$pack_sha" <<'PY'
          import json
          import pathlib
          import sys

          provider_id, version_name, version_code, asset_url, pack_sha = sys.argv[1:]
          catalog_path = pathlib.Path("catalog/catalog.json")
          catalog = json.loads(catalog_path.read_text(encoding="utf-8"))
          manifest_path = pathlib.Path(f"providers/{provider_id}/provider.json")
          manifest = json.loads(manifest_path.read_text(encoding="utf-8"))
          if provider_id == "qqmusic":
              catalog["providers"] = [
                  item for item in catalog["providers"] if item["id"] != "qqmusic-hd"
              ]
          canonical_display_names = {
              "qqmusic": "QQ音乐",
              "kugou": "酷狗音乐",
              "lxmusic": "LX音乐",
              "qishui": "汽水音乐",
              "salt-player": "椒盐音乐",
              "spotify": "Spotify",
          }
          for item in catalog["providers"]:
              if item["id"] in canonical_display_names:
                  item["displayName"] = canonical_display_names[item["id"]]
          provider = next(item for item in catalog["providers"] if item["id"] == provider_id)
          provider.update(
              targetPackages=manifest["targetPackages"],
              available=True,
              versionName=version_name,
              versionCode=int(version_code),
              assetUrl=asset_url,
              sha256=pack_sha,
          )

          lines = [
              "{",
              f'  "schemaVersion": {catalog["schemaVersion"]},',
              '  "providers": [',
          ]
          for index, item in enumerate(catalog["providers"]):
              suffix = "," if index + 1 < len(catalog["providers"]) else ""
              encoded = json.dumps(item, ensure_ascii=False, separators=(",", ":"))
              lines.append(f"    {encoded}{suffix}")
          lines.extend(["  ]", "}"])
          catalog_path.write_text("\n".join(lines) + "\n", encoding="utf-8")
          PY

          GH_TOKEN='${{ github.token }}' python3 scripts/verify_catalog_assets.py \
            --catalog catalog/catalog.json \
            --repository "$GITHUB_REPOSITORY"

          signature_file=$(mktemp)
          openssl pkeyutl -sign -rawin \
            -inkey signing-private.pem \
            -in catalog/catalog.json \
            -out "$signature_file"
          base64 -w0 "$signature_file" > catalog/catalog.sig
          printf '\n' >> catalog/catalog.sig
          openssl pkeyutl -verify -rawin -pubin \
            -inkey keys/provider-signing-public.pem \
            -in catalog/catalog.json \
            -sigfile "$signature_file"

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add catalog/catalog.json catalog/catalog.sig
          if git diff --cached --quiet; then
            exit 0
          fi
          git commit \
            -m "更新 $provider Provider 发布目录" \
            -m "- 同步 $version_name 版本与 CI 生成的 Pack 摘要。" \
            -m "- 使用项目 Ed25519 私钥重新签名官方目录。"
          git push origin main
      # 同步到 Cloudflare R2（App 端回退源）。桶内只保留目录引用的最新插件包，
      # 历史版本以 GitHub Releases 为准；上传后按 catalog 反查引用的 tag，删除其余目录。
      # 需在仓库 Secrets 配置 R2_ACCOUNT_ID / R2_ACCESS_KEY_ID / R2_SECRET_ACCESS_KEY，
      # 可选 Variable R2_BUCKET（默认 hle-plugins）。对象键复刻仓库相对路径：
      # catalog/catalog.json(.sig) 与 releases/download/<tag>/<file>。
      - name: Sync catalog and latest packs to Cloudflare R2
        env:
          R2_ACCOUNT_ID: ${{ secrets.R2_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
        run: |
          : "${R2_ACCOUNT_ID:?missing secret R2_ACCOUNT_ID}"
          : "${R2_ACCESS_KEY_ID:?missing secret R2_ACCESS_KEY_ID}"
          : "${R2_SECRET_ACCESS_KEY:?missing secret R2_SECRET_ACCESS_KEY}"
          bucket="${{ vars.R2_BUCKET }}"
          : "${bucket:=hle-plugins}"
          export RCLONE_CONFIG_R2_TYPE=s3
          export RCLONE_CONFIG_R2_ENDPOINT="https://${R2_ACCOUNT_ID}.r2.cloudflarestorage.com"
          export RCLONE_CONFIG_R2_ACCESS_KEY_ID="$R2_ACCESS_KEY_ID"
          export RCLONE_CONFIG_R2_SECRET_ACCESS_KEY="$R2_SECRET_ACCESS_KEY"
          release_path="releases/download/${{ steps.pack.outputs.release_tag }}"
          mkdir -p "$release_path"
          cp '${{ steps.pack.outputs.asset }}' "$release_path/"
          rclone copy catalog "R2:${bucket}/catalog"
          rclone copy "$release_path" "R2:${bucket}/${release_path}"
          python3 - <<'PY' > /tmp/referenced_tags.txt
          import json

          catalog = json.load(open("catalog/catalog.json", encoding="utf-8"))
          for item in catalog["providers"]:
              parts = (item.get("assetUrl") or "").rstrip("/").split("/")
              if len(parts) >= 3 and parts[-3] == "download":
                  print(parts[-2])
          PY
          rclone lsf "R2:${bucket}/releases/download" --dirs-only \
            | sed 's:/*$::' | sort -u > /tmp/remote_tags.txt
          comm -23 /tmp/remote_tags.txt <(sort -u /tmp/referenced_tags.txt) \
            | while IFS= read -r stale; do
                [ -n "$stale" ] || continue
                echo "Pruning stale pack: $stale"
                rclone purge "R2:${bucket}/releases/download/$stale"
              done
          echo "Synced catalog/ and ${release_path}/ to R2 bucket: ${bucket} (latest-only)"
